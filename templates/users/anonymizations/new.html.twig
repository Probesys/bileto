{#
 # This file is part of Bileto.
 # Copyright 2022-2026 Probesys
 # SPDX-License-Identifier: AGPL-3.0-or-later
 #}

{% extends 'modal.html.twig' %}

{% block title %}{{ 'anonymizations.new.title' | trans }}{% endblock %}

{% block body %}
    {{ form_start(form, {
        action: path('new user anonymization', { uid: user.uid }),
    }) }}
        {{ include('alerts/_alert.html.twig', {
            type: 'warning',
            title: 'common.caution' | trans,
            message: 'anonymizations.new.information' | trans({ user: user.displayName }),
        }, with_context = false) }}

        {{ form_errors(form) }}
    {{ form_end(form) }}
{% endblock %}
