{#
 # This file is part of Bileto.
 # Copyright 2022-2023 Probesys
 # SPDX-License-Identifier: AGPL-3.0-or-later
 #}

{% extends 'base.html.twig' %}

{% if type == 'admin' %}
    {% set pageTitle = 'New admin role' | trans %}
{% else %}
    {% set pageTitle = 'New orga role' | trans %}
{% endif %}

{% block title %}{{ pageTitle }}{% endblock %}

{% block sidebar %}
    {{ include('_sidebar_settings.html.twig', { current: 'roles' }, with_context = false) }}
{% endblock %}

{% block body %}
    <main class="layout__body flow">
        <div class="layout__body-header">
            <a href="{{ path('roles') }}">{{ 'Roles' | trans }}</a>
            <h1>{{ pageTitle }}</h1>
        </div>

        {{ include('roles/_role_form.html.twig', { mode: 'new' }) }}
    </main>
{% endblock %}
