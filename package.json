{
  "name": "bileto",
  "license": "AGPL-3.0-or-later",
  "repository": "github:Probesys/bileto",
  "scripts": {
    "watch": "vite build --config .vite.config.js --mode development --watch",
    "build": "vite build --config .vite.config.js"
  },
  "devDependencies": {
    "vite": "^3.1.0"
  }
}
